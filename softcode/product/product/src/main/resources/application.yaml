spring:
  application:
    name: product-service
server:
  port: 8081

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: test
    password: ""

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create