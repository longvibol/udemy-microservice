spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/longvibol/app-configuration.git
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
          default-label: main
          clone-on-start: true

server:
  port: 8888